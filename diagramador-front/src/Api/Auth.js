// src/Api/Auth.js
// Aquí puedes agregar funciones de autenticación si tu backend lo requiere

const Auth = {
	// login: async (credentials) => { ... },
	// logout: () => { ... },
	// getToken: () => { ... },
};

export default Auth;
